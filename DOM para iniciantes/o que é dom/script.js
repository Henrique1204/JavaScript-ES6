const hrefPagina = window.location.href;
alert(hrefPagina);

const primeiroElementoAtivo = document.querySelectorAll(".ativo")[0];
alert(primeiroElementoAtivo.innerText);

const linguagemNavegador = window.navigator.language;
alert(linguagemNavegador);

const larguraJanela = window.screen.width;
alert(larguraJanela);